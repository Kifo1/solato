server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  application:
    name: solato
  profiles:
    active: dev

solato:
  jwt:
    secret: ${JWT_SECRET:90967723bc52d3ee508672781442d898}